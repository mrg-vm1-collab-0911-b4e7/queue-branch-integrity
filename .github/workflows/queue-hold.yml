name: Queue Hold
on:
  pull_request:
jobs:
  queue-hold:
    runs-on: ubuntu-latest
    steps:
      - name: Hold then fail for VM1 Auto-Retry control
        run: |
          echo "VM1_RETRY_RULE_FRESHNESS_20260916"
          sleep 15
          exit 1
